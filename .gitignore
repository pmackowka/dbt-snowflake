# dbt — artefakty budowania i lokalne pliki
target/
dbt_packages/
logs/
.user.yml

# Poświadczenia — w repo tylko profiles.yml.example. Przy key-pair auth sekretem jest plik
# klucza prywatnego, nie hasło w profilu - ignore to siatka na klucz wrzucony do folderu repo.
profiles.yml
*.p8
*.pem
rsa_key*

# Python
__pycache__/
*.py[cod]
venv/
.venv/
.env

# macOS
.DS_Store
